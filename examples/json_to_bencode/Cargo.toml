[package]
name = "json_to_bencode"
version = "0.2.0"
edition = "2024"

[dependencies]
json_lib = { path = "../../library/", features = ["format-bencode"] }
json_utility_lib = { path = "../json_utility_lib" }
