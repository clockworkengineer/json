[package]
name = "json_pretty_and_safe_access_demo"
version = "0.1.7"
edition = "2024"

[dependencies]
json_lib = { path = "../../library", features = [
    "std",
    "alloc",
    "json-pointer",
] }
